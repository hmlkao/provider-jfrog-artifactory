apiVersion: remote.artifactory.jfrog.crossplane.io/v1alpha1
kind: GemsRepository
metadata:
  name: remote-gems-repository-example
spec:
  forProvider:
    key: "remote-gems-repo"
    url: "https://gems.example.com"
    description: "Example Gems remote repository"
  providerConfigRef:
    name: default
