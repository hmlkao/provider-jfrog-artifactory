apiVersion: remote.artifactory.jfrog.crossplane.io/v1alpha1
kind: ConanRepository
metadata:
  name: conan-remote-repository
spec:
  forProvider:
    # key: conan-remote-repository
  providerConfigRef:
    name: default
