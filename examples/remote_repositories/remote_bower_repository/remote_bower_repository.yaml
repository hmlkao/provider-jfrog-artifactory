apiVersion: remote.artifactory.jfrog.crossplane.io/v1alpha1
kind: BowerRepository
metadata:
  name: bower-remote-repository
spec:
  forProvider: {}
    # key: bower-remote-repository
  providerConfigRef:
    name: default
