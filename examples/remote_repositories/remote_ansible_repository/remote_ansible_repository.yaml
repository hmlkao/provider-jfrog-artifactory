apiVersion: remote.artifactory.jfrog.crossplane.io/v1alpha1
kind: AnsibleRepository
metadata:
  name: ansible-remote-repository
spec:
  forProvider: {}
    # key: ansible-remote-repository
  providerConfigRef:
    name: default
