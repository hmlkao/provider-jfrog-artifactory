apiVersion: remote.artifactory.jfrog.crossplane.io/v1alpha1
kind: ComposerRepository
metadata:
  name: composer-remote-repository
spec:
  forProvider: {}
    # key: composer-remote-repository
  providerConfigRef:
    name: default
