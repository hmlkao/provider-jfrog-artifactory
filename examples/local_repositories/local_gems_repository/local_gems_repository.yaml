apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: GemsRepository
metadata:
  name: gems-crossplane-repository
spec:
  forProvider: {}
    # key: gems-crossplane-repository
  providerConfigRef:
    name: default
