apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: ChefRepository
metadata:
  name: chef-crossplane-repository
spec:
  forProvider: {}
    # key: chef-crossplane-repository
  providerConfigRef:
    name: default
