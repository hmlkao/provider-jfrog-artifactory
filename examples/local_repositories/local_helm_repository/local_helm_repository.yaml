apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: HelmRepository
metadata:
  name: helm-crossplane-repository
spec:
  forProvider: {}
    # key: helm-crossplane-repository
  providerConfigRef:
    name: default
