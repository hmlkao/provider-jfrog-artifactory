apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: NpmRepository
metadata:
  name: npm-crossplane-repository
spec:
  forProvider: {}
    # key: npm-crossplane-repository
  providerConfigRef:
    name: default
