apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: PuppetRepository
metadata:
  name: puppet-crossplane-repository
spec:
  forProvider: {}
    # key: puppet-crossplane-repository
  providerConfigRef:
    name: default
