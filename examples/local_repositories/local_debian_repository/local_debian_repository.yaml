apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: DebianRepository
metadata:
  name: debian-crossplane-repository
spec:
  forProvider: {}
    # key: debian-crossplane-repository
  providerConfigRef:
    name: default
