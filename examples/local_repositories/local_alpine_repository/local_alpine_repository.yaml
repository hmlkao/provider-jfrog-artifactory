apiVersion: local.local.artifactory.jfrog.crossplane.io/v1alpha1
kind: AlpineRepository
metadata:
  name: alpine-crossplane-repository
spec:
  forProvider: {}
    # key: alpine-crossplane-repository
  providerConfigRef:
    name: default
