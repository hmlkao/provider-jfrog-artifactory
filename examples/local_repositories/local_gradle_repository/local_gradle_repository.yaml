apiVersion: local.artifactory.jfrog.crossplane.io/v1alpha1
kind: GradleRepository
metadata:
  name: gradle-crossplane-repository
spec:
  forProvider: {}
    # key: gradle-crossplane-repository
  providerConfigRef:
    name: default
